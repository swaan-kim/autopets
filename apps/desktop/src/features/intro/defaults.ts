import type { ArtifactSnapshot } from '@autopets/contracts/types';
export const EMPTY_ARTIFACTS: ArtifactSnapshot = { observedChats: [], projects: [], styles: [], capabilities: { automaticGeneration: false, modelSwitch: false, liveConnectionVerified: false } };
