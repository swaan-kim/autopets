# AutoPets 전체 기능 최소 실증 실행

## 재개 범위: 외부 연결 0–3 (2026-09-26)

사용자가 승인한 이번 묶음은 미완성 코드·패키지 정리 → 실제 연결 → 모델/추론 변경 → 첫 지침·native Plan·계획 프롬프트·제출 보호까지다. 이후 펫 연출이나 꾸미기로 자동 진행하지 않는다. 실제 AI 누계 6/30에서 추가 최대 12턴(이번 종료 시 최대 18/30)을 사용한다. 현재 작업 정보 조회는 기존 채팅 제어·실시간 구독의 증거가 아니다.

- Codex 로컬, Work 로컬, Work 클라우드, Chat 앱, Chat 웹을 각각 판정한다. 공식 외부 인터페이스, 배포 가능한 플러그인/확장, UI 자동화 순으로 확인한다.
- `thread/settings/update`는 로컬 프로토콜에 존재하는 시험 후보다. 별도 서버에서만 바뀌거나 내부 Codex 도구로만 성공하면 외부 Desktop 제어는 미통과로 남긴다.
- 첫 지침은 새 빈 시험 작업에서 첫 모델 실행 전에 전달돼야 한다. native Plan 상태/실행 제한과 계획 프롬프트의 전달/이행을 별도 판정한다.
- 동일 원인 수정 후 최대 두 번 재시험한다. 로그인·신뢰·보안 검토는 사용자 몫이며, 막힌 기능의 독립 코드 검사는 계속한다.
- 0단계에서 새 runtime/조회 CLI의 설치 패키지 누락을 수정하고, 메타데이터 가져오기·계층 표시·Work 상태 격리 검사를 보완했다. 전체 Node 173건, UI(pageErrors 0), 빌드·레이아웃은 통과했다. Windows 검사 결과는 해당 커밋에 별도로 연결한다.

2026-09-25 승인 계획. Jev는 보류한다. 기반 커밋은 `997a941`이며 기존 작업 공간을 보존한 `feat/mvp-feasibility`에서 진행한다. 현재 PC 설치 1단계는 이미 통과했다. 영향받는 코드가 바뀔 때만 회귀 검사한다.

## 순서와 통과 관문

| 순서 | 최소 결과 | 통과에 필요한 실제 증거 |
| --- | --- | --- |
| 2 연결 | 호스트·출처·채팅·턴 식별, 중복/지연 격리 | 전용 A/B의 시작·도구·완료가 각각 정확하게 전달 |
| 3 라우팅 | 가용 모델 조회, 희망/관측/실행값 분리, 안전한 변경 | 기존 Desktop 채팅 설정 재조회 3회 일치 + A/B 실행값 확인 |
| 4 지침·Plan·제출 | 3KB 이하 지침, 개정별 확인, 보류/재제출 | 첫 턴 전달, native Plan, 원문/첨부 보존, 정확히 1회 실행 |
| 5 펫 | 드래그/숨김/복원/입력/위치, 작업으로 돌아가기 | 실제 상태와 펫 일치, 정확한 기존 채팅 ID로 복귀 |
| 6 내 펫 | 조사·문서/제작·구현 역할 저장·재사용, 모델 자료 | 재시작 복원·다른 채팅 격리·검증된 경로의 설정 적용 |
| 7 선택창 | 대상 좌표 추적, 확인 후 단 한 번 반응 | 입력 비간섭, 실패/중복에 성공 연출 없음 |
| 8 확장 | 프로젝트/채팅/자식, 소품/배경, MCP 상태 | 관계와 사용 이벤트 정확, 저장 복원 |
| 9 통합 | 두 과제 × 세 방식, 자원/복구/배포 평가 | 조건·한계·전체 비용과 제품 가능성 판정 |

막힌 경로의 의존 기능만 대기하고 독립 검사는 계속한다. 같은 원인은 수정 후 최대 두 번만 재시험한다. 실제 앱 조작과 호스트 변경은 한 담당자가 순차 수행한다. 훅 신뢰·로그인·서명·보안 검토를 우회하지 않는다.

## 환경별 독립 판정

사용자 추가 확인: Codex뿐 아니라 **ChatGPT Work와 일반 ChatGPT Chat**도 실제 가능성을 확인한다. `provider=openai`라는 공통점만으로 설정이나 지원 상태를 공유하지 않는다.

| 대상 | 구분해서 확인할 항목 |
| --- | --- |
| Codex Windows 로컬 | 작업 식별·이벤트·모델/추론·Plan·지침·제출·복귀 |
| ChatGPT Work 로컬 | 같은 항목을 별도 작업/호스트에서 확인. Codex 결과 전용 금지 |
| ChatGPT Work 웹/클라우드 | 로컬 Work와 별도 연결 수단·접근 조건·실행 범위 |
| 일반 ChatGPT Chat 웹 | 실제 로그인/작업/모델 식별, 확장과 입력 보존, 가능한 설정 범위 |
| 일반 ChatGPT Chat 앱 | 웹/Work와 분리해 UI 접근·작업 식별·설정/지침·복귀 확인 |

모든 환경의 실제 시험은 기존 총 30턴 한도 안에 포함한다. 기본 Work 2턴을 유지하되 Chat 실증은 사용하지 않은 핵심·복구 예산을 사전 재배분해 기록한다. GUI 중단 상태에서는 코드·공식 문서·연결 계약 검사부터 계속하고 로그인/GUI 실증을 통과로 처리하지 않는다.

Work·일반 Chat의 연결 가능성 조사는 9단계까지 미루지 않는다. 2단계에서 각 환경의 공식 연결 경로·대상 식별을 먼저 판정하고, 가능한 경로에만 3–4단계의 모델/추론·지침·Plan·제출 검사를 적용한다. 같은 OpenAI 계정이나 공통 런타임을 사용하더라도 다른 환경의 통과 결과를 상속하지 않는다. 일반 Chat에 native Plan이나 훅이 없으면 해당 기능은 별도로 미지원/미확인으로 기록하고, 계획 지침 제공과 구분한다.

## 예산과 증거

실제 AI 시험은 최대 **30턴**(Codex 핵심 16, 비교 10, Work 2, 복구 2). 재시도·계획·실행·시험 자식도 포함하며, 미사용분은 전용할 수 있다. 개발 검사·빌드·읽기 전용 RPC는 AI 턴이 아니다. 실제 요청 전에 로컬 원장에 예약하여 불확실한 전송도 이중 사용하지 않는다.

원본 진단과 턴 원장·재개 위치는 Git에서 제외한 `work/mvp-goal/20260925-minimum/`에 둔다. 전체 대화나 인증값은 보고서에 포함하지 않는다. [판정 기록](../testing/mvp-feasibility.md)은 환경·버전·커밋·입력·예상·실제·증거 수준·다음 조건을 구분한다.

## 완료 기준

각 항목에 최소 실험 결과 또는 구체적인 외부 차단 조건이 있고, 실행 가능한 검사와 관련 회귀 검사가 끝나야 한다. 미검증 클라이언트가 지원 상태를 올릴 수 없어야 한다. 사용 가능한 앱, 검토 가능한 코드, 재현 명령, 다음 시작 지점을 남긴다. 최소 실증 완료와 공개 제품 완성을 구분한다.

## 2026-09-26 체크포인트

- `826af68` 설치본의 Windows Rust·실제 역할 저장·종료·재설치 보존까지 CI 통과. 현재 PC의 기존 설치본과 구분한다.
- Codex A의 UI 모델·추론 변경과 다음 턴의 런타임 선택을 관측했다. 원래 Astra Ultra 복원 및 B의 선택 유지도 확인했다. 외부 AutoPets 기존 채팅 제어는 여전히 미통과다.
- 일반 Chat 앱의 별도 모델 선택기와 합성 요청/응답, Work 로컬의 합성 파일 읽기와 완료를 확인했다. Work의 런타임 originator와 Codex를 구분하며 공통 내부 kind를 지원 판정에 사용하지 않는다.
- 일반 Chat 웹은 별도 로그인 필요. 실제 화면 검사는 허가된 전용 작업만 사용하며, 원장에 턴 예약을 기록한다.
- 다음 핵심 작업은 정확한 호스트·채팅별 연결/제어 계약과 실제 훅 수신, 첫 지침/Plan 실증이다. 독립적으로 5의 직접 복귀, 7의 좌표·확인 후 모션, 8의 계층·MCP 상태, 9의 비교·자원 측정도 남아 있다. 이번 체크포인트를 전체 최소 실증 완료로 계산하지 않는다.
- `e242f55`도 Windows 설치 수명주기 통과. 짧은 CI 자원 표본을 확보했으며 장시간 대기/일반 PC 성능으로 확대하지 않는다.
- 공식 로컬 작업 링크의 요청 코드를 구현했다. 현재 PC 브라우저 정책 차단으로 실제 도착 검사는 미통과이고 우회하지 않는다. 명령 전달과 복귀 성공을 구분한다.
- 전용 A/B 정의 각 8건을 별도 런타임이 발견했으나 모두 미신뢰다. Desktop의 원래 개발 작업 범위 Hook 화면은 비어 있으므로 전용 작업 범위의 발견/검토 화면부터 확인한다. 신뢰 검토 요청을 이해하지 못했다는 사용자 답변은 승인으로 간주하지 않는다. 화면 조작 중 사용자 입력 감지 시 경쟁하지 말고 독립 검사를 계속한다.
- 전용 A의 Hook도 비어 있었고, 정확한 시험 폴더를 열자 폴더 신뢰 안내가 나타나 사용자에게 넘겼다. 이는 폴더 읽기/수정/실행 권한이며 단순 알림 허용과 다르다. 별도 승인 답변 없이 다음 권한 단계로 진행하지 않는다.
- 독립 작업으로 도구 호출별 상태와 MCP 소품을 추가했다. 완료 후 사용 중 표시, 늦은 병렬 이벤트, 재시작 후 미확인 상태, A/B 격리·저장 실패 롤백을 검사한다. 실제 MCP 수신과 가용성 조회는 별도 미검증이다.
- `901abf4` Windows Rust 107건 및 설치 수명주기 통과. 이후 MCP 상태 변경의 로컬 Node/UI/빌드는 통과했으며 새 Rust·설치 검사는 다음 CI로 구분한다.
